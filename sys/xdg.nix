{
  xdg = {
    terminal-exec = {
      enable = true;

      settings = {
        default = [
          "kitty.desktop"
        ];

        Hyprland = [
          "kitty.desktop"
        ];
      };
    };


    mime.enable = true;
    icons.enable = true;
    menus.enable = true;
    sounds.enable = true;
    autostart.enable = true;
    portal.wlr.enable = true;
  };
}
