lib: {
  normal = {
    "<leader>tt" = {
      action = "<cmd>tabnew<CR>";
      desc = "Create new Tab";
    };

    "<leader>th" = {
      action = "<cmd>tabprev<CR>";
      desc = "Cycle to Previous Tab";
    };

    "<leader>tl" = {
      action = "<cmd>tabnext<CR>";
      desc = "Cycle to Next Tab";
    };

    "<leader>ty" = {
      action = "<cmd>tabs<CR>";
      desc = "List Tabs";
    };

    "<leader>tO" = {
      action = "<cmd>tabonly<CR>";
      desc = "Close Other Tabs";
    };

    "<leader>tm" = {
      action = "<cmd>tabmove<CR>";
      desc = "Move Current Tab To...";
    };
  };
}
